
// This is a wrapper component that re-exports the refactored InstitutionDialog
// to maintain backward compatibility
import InstitutionDialog from "./institution-dialog";
export default InstitutionDialog;
